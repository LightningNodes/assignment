/**
 * Enum for AuthType to be used in the Auth component
 * @enum {string} AuthType - to store the types for the Auth component
 */

export enum AuthType {
    Login = 'Login',
    Register = 'Register',
}